Something suspicious is happening with the query parameters when accessing different files.

The `s` query parameter is different for each file, but the `r` parameter stays the same.

What are `s` and `r` and how are they created and used?
